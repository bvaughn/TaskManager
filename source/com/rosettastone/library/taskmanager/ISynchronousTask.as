package com.rosettastone.library.taskmanager {
	
	/**
	 * A synchronous Task is one that completes immediately upon being run.
	 * 
	 * @internal
	 * This interface is a useful placeholder even though it does not define any additional API methods.
	 */
	public interface ISynchronousTask extends ITask {
		
	}
}